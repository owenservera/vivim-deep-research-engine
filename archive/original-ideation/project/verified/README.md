# Verified

Candidates that received a `SURVIVED VALIDATION` verdict from a validator agent
are written up here as standalone documents by a synthesizer agent, aimed at a
competent expert in the field.

A file here is NOT proof of correctness. It means no subagent could break the
candidate. Independent human expert review (Phase 4) is still required before
it is treated as established.
