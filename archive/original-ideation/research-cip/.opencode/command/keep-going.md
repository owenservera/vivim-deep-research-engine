---
description: Persistence follow-up for when the fleet reports being stuck or thinks the CIP problem is intractable. Do not feed new technical direction — just reinforce continuation.
---

Keep going — trust yourself and the process.

This is not a platitude, it's load-bearing. The only human intervention in the real Riemann run between total failure (650 dead ideas) and the eventual result was "try again / keep going / trust yourself" — no technical hints. The model's skepticism about "open problems are basically unsolvable by me" is itself an obstacle.

Try combinations you haven't tried yet. For CIP, also try:
- a different CIP subsystem than the ones the fleet has focused on (indexer vs retrieve vs audit vs watcher),
- a different file tier or language (code vs doc vs config, Python vs TypeScript vs .md),
- a Windows-specific edge (case-insensitive path, `C:\` with spaces, backslash vs forward slash),
- a different seeded hazard from HAZARDS.md than the fleet has been exercising.

Do not change FRAMING.md strategy — just persist the fleet with fresh specialization.

